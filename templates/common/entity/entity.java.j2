package {{ base_package }}.{{ module_name }}.entity;

{% set is_map_entity = entity_name.endswith('Map') or entity_name.endswith('Log') -%}
import {{ common_package }}.entity.{% if is_map_entity %}BaseMapEntity{% else %}BaseEntity{% endif %};
import jakarta.persistence.*;
import jakarta.validation.constraints.NotNull;
import io.swagger.v3.oas.annotations.media.Schema;
import lombok.*;

import java.time.OffsetDateTime;
import java.time.LocalDate;
import java.math.BigDecimal;

/**
 * 테이블명: {{ table_name }}
 * 설명: {{ table_comment if table_comment else entity_name }}
 */
@Entity
@Table(name = "{{ table_name }}", schema = "portal")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
@Schema(description = "{{ table_comment | e }}")
public class {{ entity_name }} extends {% if is_map_entity %}BaseMapEntity{% else %}BaseEntity{% endif %} {

{% for field in fields -%}
    {%- if field.comment %}
    @Schema(description = "{{ field.comment | e }}")
    {%- endif %}
    {%- if field.is_pk %}
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    {%- endif %}
    {%- if not field.is_nullable and not field.is_pk %}
    @NotNull
    {%- endif %}
    @Column(name = "{{ field.name }}"{% if not field.is_nullable %}, nullable = false{% endif %})
    private {{ field.type }} {{ field.field_name }};

{% endfor -%}
}